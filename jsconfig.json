{
    "compilerOptions": {
      "baseUrl": "src",  // Points to the 'src' folder directly.
      "paths": {
        "@/*": ["./*"]  // Resolves '@' to 'src' folder correctly.
      }
    },
    "include": [
      "src/**/*"  // Make sure the entire 'src' folder is included.
    ]
  }
  